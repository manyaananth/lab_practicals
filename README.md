# lab_practicals
#i have created a new repo and added readme and license to the repo
#then cloned the repo using the url in the git bash.
# initially i have created the python files and added them using add command and committed with a message the pushed it to the repo
# the  created a seperate branch named feature- branch then did some changes in the feature branch and then merged them using the merge command in the main 
# main amd master has no technical difference just the name conversion.
# Then created a tag v1.0 and the pushed it to the repo.
